# Usage

```
  packer build \
  -var 'aws_access_key={your_aws_access_key}' \
  -var 'aws_secret_key={your_aws_secret_key}' \
  -var 'subnet_id={your_aws_secret_key}' \
  -var 'security_group_ids={your_security_group_ids}'
  trusty64.json
```
